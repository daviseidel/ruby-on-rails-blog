<head>
  <link rel="stylesheet" href=" https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"  crossorigin="anonymous">
</head>

<%= render "header"%>

<h1 class="display-1"><%= @artigo.titulo %></h1>
<p class="h5"><%= @artigo.corpo %></p>

<%= link_to "Editar", edit_article_path(@artigo), :class => "btn btn-primary"%>
<%= button_to "Apagar", article_path(@artigo), method: :delete, data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  },
    :class => "btn btn-danger"%>
<% link_to "Home", root_path %>